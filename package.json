{
  "name": "lambda-toolkit",
  "version": "0.0.16",
  "description": "Build repository for lambda-toolkit modules",
  "author": {
    "name": "Ben Sammons"
  },
  "license": "MIT",
  "scripts": {
    "install": "cd packages/lambda-toolkit-api-composer && npm i && cd ../lambda-toolkit-utilities && npm i",
    "build": "tsc -b --incremental packages/*",
    "clean": "tsc -b --clean packages/*"
  },
  "devDependencies": {},
  "dependencies": {
    "@types/node": "^13.1.8",
    "prettier": "^1.19.1",
    "typescript": "^3.7.4"
  }
}